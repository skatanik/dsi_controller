# vlog -v -incr -work work $defines $include_dir uart_regs.v
# vlog -v -incr -work work $defines $include_dir uart_wrapper.v
# vlog -v -incr -work work $defines $include_dir verilog-uart/rtl/uart.v
# vlog -v -incr -work work $defines $include_dir verilog-uart/rtl/uart_rx.v
# vlog -v -incr -work work $defines $include_dir verilog-uart/rtl/uart_tx.v
